# مترجم-مورس

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![MIT License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![FastAPI](https://img.shields.io/badge/fastapi-latest-009485.svg)](https://fastapi.tiangolo.com/)

[نسخه فارسی سند](../docs/README.fa.md)\
[English Version of the Doc](../README.md)\
[Version française du document](../docs/README.fr.md)

مترجم مدمج وغني بالميزات لتحويل شفرة مورس ⇄ نص مع واجهة سطر أوامر CLI وواجهة رسومية GUI وواجهة برمجة تطبيقات REST. تحويلات ثنائية الاتجاه للحروف والأرقام وعلامات الترقيم الشائعة مع تحقق مدمج.

---

## 📸 لقطات شاشة

<div style="
  display:flex;
  gap:12px;
  overflow-x:auto;
  padding:12px 0;
  flex-wrap:nowrap;
">
  <img src="../screenshots/1.png" style="max-height:420px; flex:0 0 auto;">
  <img src="../screenshots/2.png" style="max-height:420px; flex:0 0 auto;">
  <img src="../screenshots/3.png" style="max-height:420px; flex:0 0 auto;">
  <img src="../screenshots/4.png" style="max-height:420px; flex:0 0 auto;">
  <img src="../screenshots/5.png" style="max-height:420px; flex:0 0 auto;">
</div>

---

## 🚀 الميزات

- **نص ↔ مورس**: تحويل ثنائي الاتجاه للحروف A–Z والأرقام 0–9 وعلامات الترقيم الشائعة
- **التحقق**: فحص صيغة مورس (المسموح: `.`, `-`, `/`, مسافة؛ الحد الأقصى 6 أحرف لكل رمز)
- **CLI تفاعلي**: تجربة قائمة تفاعلية عبر `main.py`
- **واجهة رسومية لسطح المكتب**: واجهة مبنية على `customtkinter` سريعة الاستجابة مع حقول إدخال نص/مورس ومفتاح تبديل
- **REST API**: باستخدام FastAPI مع مسارات مُنسخة (`/v1`) وتوثيق تفاعلي في `/docs`
- **حزمة قابلة للتثبيت**: أمر `morse` عام بعد `pip install -e .`
- **أعلام CLI**: تحويلات فورية (`-t`, `-m`, `-vm`, `-ui`)

---

## 📦 التثبيت

1. **الاستنساخ وإنشاء بيئة افتراضية:**

```bash
git clone https://github.com/amirhossein77-98/Morse-Translator.git
cd Morse-Translator
python3 -m venv .venv
source .venv/bin/activate  # على ويندوز: .venv\Scripts\activate
```

2. **تثبيت الاعتمادات:**

```bash
pip install -e .
```

يقوم بتثبيت المشروع في وضع التطوير مع نقطة دخول CLI عامة باسم `morse`.

**بيئات بديلة:**
- **pipenv:** `pipenv install --dev && pipenv shell`
- **poetry:** `poetry install && poetry shell`
- **uv:** `uv sync` (إذا تم تهيئته باستخدام uv)

---

## 🖥 الاستخدام

### التشغيل المباشر

**قائمة تفاعلية:**
```bash
python3 main.py
```

### وسائط CLI

جميع الأعلام متنافية. استخدم ` / ` (مسافة-شرطة مائلة-مسافة) للفصل بين الكلمات في مورس.

**من نص إلى مورس:**
```bash
python3 main.py -t "Hello World"
morse -t "Hello World"  # بعد pip install -e .
```

**من مورس إلى نص:**
```bash
python3 main.py -m ".... . .-.. .-.. --- / .-- --- .-. .-.. -.."
```

**التحقق من مورس:**
```bash
python3 main.py -vm ".... . .-.. .-.. ---"
```

### الواجهة الرسومية GUI

شغّل واجهة `customtkinter` سريعة الاستجابة:

```bash
python3 main.py -ui
morse -ui  # بعد pip install -e .
```

**الميزات:**
- التبديل بين "نص إلى مورس" و"مورس إلى نص" عبر مفتاح
- مربعات نص إدخال/إخراج آنية
- التحقق من إدخال مورس
- تخطيط شبكي سريع الاستجابة مع عناصر مُتمركزة

### واجهة البرمجة API

ابدأ خادم FastAPI:

```bash
uvicorn api.app:app --reload
```

افتح `http://127.0.0.1:8000/docs` للاطلاع على توثيق API التفاعلي.

**أمثلة على النقاط الطرفية** (الأساس: `/v1`):

```bash
# GET
curl "http://127.0.0.1:8000/v1/text-to-morse/hello"
curl "http://127.0.0.1:8000/v1/morse-to-text/--"
curl "http://127.0.0.1:8000/v1/validate-morse/--"

# POST
curl -X POST "http://127.0.0.1:8000/v1/text-to-morse" \
  -H "Content-Type: application/json" \
  -d '{"text":"hello"}'
```

صيغة الاستجابة:
```json
{
  "original_text": "hello",
  "translated_text": ".... . .-.. .-.. ---"
}
```

### حزمة `morse` القابلة للتثبيت

بعد `pip install -e .`، استخدم الأمر العام `morse`:

```bash
morse -t "Hello"
morse -m ".... . .-.. .-.. ---"
morse -vm "..."
morse -ui
```

---

## 🔧 الاختبار

شغّل اختبارات الوحدات وAPI (لا حاجة لتشغيل الخادم):

```bash
python3 -m unittest discover -v        # جميع الاختبارات
python3 -m unittest test.api_tests     # API فقط
python3 -m unittest test.converter_tests  # المحوّل فقط
```

**ملاحظة:** تستخدم الاختبارات `unittest` وFastAPI `TestClient`. تأكد من عدم وجود محارف Unicode غير مقصودة في سلاسل الاختبار.

---

## 🛠 التحزيم والمساهمة

**بناء حِزم التوزيع:**

```bash
pip install build
python -m build
```

ينشئ ملفات wheels وتوزيعات المصدر في `dist/`.

**بنية المشروع:**
- `core/converters.py` — منطق التحويل والتحقق
- `data/morse_dataset.py` — ربط مورس/ASCII
- `api/app.py` — تطبيق FastAPI
- `api/routes/v1.py` — نقاط طرفية مُنسخة
- `views/ui/ctkinter_ui.py` — واجهة رسومية باستخدام `customtkinter`
- `test/` — اختبارات الوحدات وAPI

**المساهمة:**
تقارير الأخطاء وطلبات السحب مرحب بها. يُرجى تضمين خطوات إعادة الإنتاج وحالات الاختبار.

---

## 📜 الترخيص

رخصة MIT — راجع [LICENSE](LICENSE) للتفاصيل.